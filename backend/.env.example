# Database connection (PostgreSQL recommended, e.g. Neon, Supabase, Render Postgres)
DATABASE_URL=postgresql://user:password@ep-xyz.neon.tech/watchtransfer?sslmode=require

# CORS Settings (optional, defaults to allowing all https domains)
CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:5173
ALLOW_ALL_HTTPS=true
